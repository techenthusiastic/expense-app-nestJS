# Environment configuration
# NODE_ENV values:
# development - local dev
# production  - live app
# test        - test environment
# staging     - pre-production
NODE_ENV=development

# ---------------------------------------
# Server Configuration
# ---------------------------------------
# SERVER_HOST: Hostname or IP address the server binds to.
# Use 'localhost' for local development.
# Use '0.0.0.0' to listen on all network interfaces (recommended for production).
# Avoid using specific IPs unless necessary.
SERVER_HOST=localhost

# SERVER_PORT: Port number your server listens on.
# Use a valid port number between 1024 and 65535.
SERVER_PORT=3000